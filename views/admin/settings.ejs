<%- include('partials/layout-header') %>

        <div class="max-w-4xl mx-auto space-y-6 mb-8">
            <div class="flex items-center space-x-3 mb-2">
                <div class="w-9 h-9 bg-indigo-50 text-indigo-600 rounded-xl flex items-center justify-center font-bold">
                    <i class="fa-solid fa-sliders text-sm"></i>
                </div>
                <div>
                    <h2 class="text-lg font-extrabold text-slate-900">Office & System Configuration</h2>
                    <p class="text-xs text-slate-500 font-medium">Configure network restrictions, shift timings, and grace periods</p>
                </div>
            </div>

            <form id="settings-form" class="space-y-6">
                <!-- ============ TOP: ACTIVE CONFIGURATION ============ -->
                <div class="surface-card p-6">
                    <!-- Card Header -->
                    <div class="flex items-center space-x-3 mb-6 pb-4 border-b border-slate-100">
                        <div class="w-10 h-10 bg-indigo-50 text-indigo-600 rounded-xl flex items-center justify-center font-bold">
                            <i class="fa-solid fa-gear text-base"></i>
                        </div>
                        <div>
                            <h2 class="text-base font-extrabold text-slate-900">Active Configuration</h2>
                            <p class="text-xs text-slate-500 font-medium">Network restriction, shift timing, and grace period</p>
                        </div>
                    </div>

                    <!-- Network Restrictions -->
                    <div class="mb-6">
                        <div class="flex items-center space-x-2 mb-4">
                            <i class="fa-solid fa-network-wired text-indigo-500 text-sm"></i>
                            <h3 class="text-sm font-extrabold text-slate-900 uppercase tracking-wider">Network Restrictions</h3>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center">
                            <div>
                                <label class="block text-slate-700 text-xs font-bold uppercase tracking-wider mb-2">Active IP Binding</label>
                                <div class="relative">
                                    <!-- View mode -->
                                    <div id="ip-view" class="flex flex-wrap items-center gap-2">
                                        <span id="current-ip" class="font-mono font-bold text-sm text-slate-700 bg-slate-100 border border-slate-200 px-3 py-2 rounded-lg">—</span>
                                        <button type="button" onclick="toggleIpEdit(true)"
                                            class="bg-indigo-50 hover:bg-indigo-100 text-indigo-700 border border-indigo-200/60 px-3 py-2 rounded-lg text-xs font-bold transition flex items-center space-x-1.5">
                                            <i class="fa-solid fa-pen text-[10px]"></i>
                                            <span>Change IP</span>
                                        </button>
                                    </div>
                                    <!-- Edit mode -->
                                    <div id="ip-edit" class="hidden">
                                        <div class="relative">
                                            <i class="fa-solid fa-globe absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400 text-xs"></i>
                                            <input type="text" id="office-ip-input"
                                                class="w-full pl-9 pr-4 py-2.5 bg-slate-50 border border-slate-200 rounded-lg text-sm font-mono focus:outline-none focus:border-indigo-500 focus:bg-white transition"
                                                placeholder="e.g. 192.168.1.100">
                                        </div>
                                        <div class="flex items-center gap-2 mt-2">
                                            <button type="button" onclick="toggleIpEdit(false)"
                                                class="bg-slate-100 hover:bg-slate-200 text-slate-600 font-bold px-3 py-1.5 rounded-lg text-xs transition">
                                                Cancel
                                            </button>
                                            <span class="text-[11px] text-slate-400 font-medium">Save changes with the button below</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="bg-slate-50 border border-slate-200/80 p-4 rounded-xl text-xs text-slate-500 space-y-1 font-medium">
                                <p class="font-bold text-slate-800 flex items-center space-x-1">
                                    <i class="fa-solid fa-shield-halved text-indigo-500 mr-1"></i>
                                    <span>IP Verification Protocol</span>
                                </p>
                                <p>Clock ins outside this public IP will be flagged as remote/unauthorized unless IP bypass is configured.</p>
                            </div>
                        </div>
                    </div>

                    <hr class="border-slate-100 mb-6">

                    <!-- Shift Schedule & Grace Period -->
                    <div>
                        <div class="flex items-center space-x-2 mb-4">
                            <i class="fa-solid fa-clock text-indigo-500 text-sm"></i>
                            <h3 class="text-sm font-extrabold text-slate-900 uppercase tracking-wider">Shift Schedule & Grace Period</h3>
                        </div>

                        <div class="bg-amber-50 border border-amber-200 rounded-xl p-3 mb-4 flex items-start space-x-2.5">
                            <i class="fa-solid fa-triangle-exclamation text-amber-600 text-xs mt-0.5"></i>
                            <p class="text-xs font-semibold text-amber-800 leading-relaxed">Changing office hours will update live attendance records immediately. Recommended to do this during non-working hours (after shift end).</p>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                            <div>
                                <label class="block text-slate-700 text-xs font-bold uppercase tracking-wider mb-2">Shift Start Time</label>
                                <input type="time" id="office-start-time"
                                    class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-lg text-sm font-bold text-slate-800 focus:outline-none focus:border-indigo-500 focus:bg-white transition">
                            </div>

                            <div>
                                <label class="block text-slate-700 text-xs font-bold uppercase tracking-wider mb-2">Shift End Time</label>
                                <input type="time" id="office-end-time"
                                    class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-lg text-sm font-bold text-slate-800 focus:outline-none focus:border-indigo-500 focus:bg-white transition">
                            </div>

                            <div>
                                <label class="block text-slate-700 text-xs font-bold uppercase tracking-wider mb-2">Grace Period (Minutes)</label>
                                <input type="number" id="grace-period" min="0" max="60"
                                    class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-lg text-sm font-bold text-slate-800 focus:outline-none focus:border-indigo-500 focus:bg-white transition">
                            </div>
                        </div>
                    </div>

                    <!-- Save Bar -->
                    <div class="flex justify-end mt-6 pt-5 border-t border-slate-100">
                        <button type="submit"
                            class="bg-slate-900 hover:bg-slate-800 text-white font-semibold px-8 py-3 rounded-xl text-sm transition shadow-sm flex items-center space-x-2">
                            <i class="fa-solid fa-floppy-disk"></i>
                            <span>Save Configuration Settings</span>
                        </button>
                    </div>
                </div>

                <!-- ============ MIDDLE: HOLIDAY CALENDAR MANAGEMENT ============ -->
                <div class="surface-card p-6">
                    <div class="flex items-center space-x-3 mb-5 pb-4 border-b border-slate-100">
                        <div class="w-10 h-10 bg-indigo-50 text-indigo-600 rounded-xl flex items-center justify-center font-bold">
                            <i class="fa-solid fa-calendar-days text-base"></i>
                        </div>
                        <div>
                            <h2 class="text-base font-extrabold text-slate-900">Holiday Calendar</h2>
                            <p class="text-xs text-slate-500 font-medium">Add public holidays so employees are not marked ABSENT on those dates.</p>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-[200px_1fr_auto] gap-4 items-end mb-5">
                        <div>
                            <label class="block text-slate-700 text-xs font-bold uppercase tracking-wider mb-2">Date</label>
                            <input type="date" id="holiday-date"
                                class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-lg text-sm font-semibold text-slate-800 focus:outline-none focus:border-indigo-500 focus:bg-white transition">
                        </div>
                        <div>
                            <label class="block text-slate-700 text-xs font-bold uppercase tracking-wider mb-2">Holiday Name</label>
                            <input type="text" id="holiday-name" placeholder="e.g. Independence Day"
                                class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-lg text-sm font-semibold text-slate-800 focus:outline-none focus:border-indigo-500 focus:bg-white transition">
                        </div>
                        <button type="button" id="add-holiday-btn"
                            class="bg-slate-900 hover:bg-slate-800 text-white font-semibold px-6 py-2.5 rounded-lg text-sm transition shadow-sm flex items-center space-x-2 justify-center">
                            <i class="fa-solid fa-plus"></i>
                            <span>Add Holiday</span>
                        </button>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full min-w-[400px] text-left text-sm">
                            <thead class="text-[11px] uppercase tracking-wider font-semibold text-slate-400 border-b border-slate-100">
                                <tr>
                                    <th class="py-2.5 px-3">Date</th>
                                    <th class="py-2.5 px-3">Name</th>
                                    <th class="py-2.5 px-3 w-16 text-right">Remove</th>
                                </tr>
                            </thead>
                            <tbody id="holidays-table" class="divide-y divide-slate-100">
                                <tr>
                                    <td colspan="3" class="py-8 text-center text-slate-400 text-xs font-medium">
                                        Loading holidays...
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- ============ BOTTOM: AUDIT LOG & SYSTEM STATUS ============ -->
                <div class="surface-card p-6">
                    <div class="flex items-center justify-between mb-5 pb-4 border-b border-slate-100 flex-wrap gap-3">
                        <div class="flex items-center space-x-3">
                            <div class="w-10 h-10 bg-indigo-50 text-indigo-600 rounded-xl flex items-center justify-center font-bold">
                                <i class="fa-solid fa-clipboard-list text-base"></i>
                            </div>
                            <div>
                                <h2 class="text-base font-extrabold text-slate-900">Audit Log & System Status</h2>
                                <p class="text-xs text-slate-400 font-medium mt-0.5">Sensitive admin actions are recorded automatically.</p>
                            </div>
                        </div>
                        <div class="flex items-center space-x-2">
                            <select id="audit-limit"
                                class="px-2.5 py-2 bg-slate-50 border border-slate-200 rounded-lg text-xs font-bold text-slate-700 focus:outline-none focus:border-indigo-500">
                                <option value="25">25</option>
                                <option value="50" selected>50</option>
                                <option value="100">100</option>
                                <option value="200">200</option>
                            </select>
                            <button type="button" id="refresh-audit-btn"
                                class="bg-slate-900 hover:bg-slate-800 text-white font-semibold px-4 py-2 rounded-lg text-xs transition shadow-sm flex items-center space-x-1.5">
                                <i class="fa-solid fa-rotate"></i>
                                <span>Refresh</span>
                            </button>
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full min-w-[700px] text-left text-sm">
                            <thead class="text-[11px] uppercase tracking-wider font-semibold text-slate-400 border-b border-slate-100">
                                <tr>
                                    <th class="py-2.5 px-3">Time</th>
                                    <th class="py-2.5 px-3">Admin</th>
                                    <th class="py-2.5 px-3">Action</th>
                                    <th class="py-2.5 px-3">Target</th>
                                    <th class="py-2.5 px-3">Status</th>
                                    <th class="py-2.5 px-3 w-10 text-right">Details</th>
                                </tr>
                            </thead>
                            <tbody id="audit-table" class="divide-y divide-slate-100">
                                <tr>
                                    <td colspan="6" class="py-8 text-center text-slate-400 text-xs font-medium">
                                        Loading audit log...
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                    <div id="audit-detail-panel" class="hidden mt-3 bg-slate-50 border border-slate-200 rounded-xl p-4">
                        <div class="flex items-center justify-between mb-2">
                            <span id="audit-detail-title" class="text-xs font-extrabold text-slate-700 uppercase tracking-wider"></span>
                            <button id="audit-detail-close" class="text-slate-400 hover:text-slate-600 text-xs font-bold"><i class="fa-solid fa-xmark"></i> Close</button>
                        </div>
                        <pre id="audit-detail-body" class="text-[11px] font-mono text-slate-600 bg-white border border-slate-200 rounded-lg p-3 overflow-x-auto whitespace-pre-wrap"></pre>
                    </div>
                </div>
            </form>
        </div>

<%- include('partials/layout-footer') %>

    <script>
        function toggleIpEdit(show) {
            const view = document.getElementById('ip-view');
            const edit = document.getElementById('ip-edit');
            if (show) {
                view.classList.add('hidden');
                edit.classList.remove('hidden');
                document.getElementById('office-ip-input').focus();
            } else {
                view.classList.remove('hidden');
                edit.classList.add('hidden');
            }
        }

        async function loadSettings() {
            try {
                const response = await fetchWithAuth(`${API_BASE}/admin/settings/office-time`);
                const data = await response.json();

                document.getElementById('office-start-time').value = data.data.office_start_time || '09:00';
                document.getElementById('office-end-time').value = data.data.office_end_time || '17:00';
                document.getElementById('grace-period').value = data.data.grace_period_minutes || 10;

                const dashResponse = await fetchWithAuth(`${API_BASE}/admin/dashboard`);
                const dashData = await dashResponse.json();
                document.getElementById('office-ip-input').value = dashData.data.office_ip;
                document.getElementById('current-ip').textContent = dashData.data.office_ip;
            } catch (err) { console.error(err); }
        }

        document.getElementById('settings-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            const ip = document.getElementById('office-ip-input').value.trim();
            const startTime = document.getElementById('office-start-time').value;
            const endTime = document.getElementById('office-end-time').value;
            const gracePeriod = document.getElementById('grace-period').value;

            if (!ip) { alert('Please enter a valid IP address'); return; }

            try {
                const ipResponse = await fetchWithAuth(`${API_BASE}/admin/settings/ip`, {
                    method: 'POST',
                    body: JSON.stringify({ office_public_ip: ip }),
                });
                const ipData = await ipResponse.json().catch(() => ({}));
                if (ipData.success === false) {
                    showMessage(ipData.message || 'Failed to update office IP', 'error');
                    loadSettings();
                    return;
                }

                const otResponse = await fetchWithAuth(`${API_BASE}/admin/settings/office-time`, {
                    method: 'POST',
                    body: JSON.stringify({
                        office_start_time: startTime,
                        office_end_time: endTime,
                        grace_period_minutes: parseInt(gracePeriod),
                    }),
                });
                const otData = await otResponse.json().catch(() => ({}));
                if (otData.success === false) {
                    showMessage(otData.message || 'Failed to update office time', 'error');
                    loadSettings();
                    return;
                }

                showMessage('Settings saved successfully', 'success');
                loadSettings();
            } catch (err) { console.error(err); }
        });

        async function loadHolidays() {
            try {
                const response = await fetchWithAuth(`${API_BASE}/admin/holidays`);
                const data = await response.json();
                const tbody = document.getElementById('holidays-table');
                if (!data.data || data.data.length === 0) {
                    tbody.innerHTML = `
                        <tr>
                            <td colspan="3" class="py-8 text-center text-slate-400 text-xs font-medium">
                                <i class="fa-solid fa-umbrella-beach text-slate-300 text-xl mb-1 block"></i>
                                <span>No holidays added yet.</span>
                            </td>
                        </tr>
                    `;
                    return;
                }
                tbody.innerHTML = data.data.map(h => `
                    <tr>
                        <td class="py-2.5 px-3 font-mono font-semibold text-slate-700">${h.date}</td>
                        <td class="py-2.5 px-3 font-semibold text-slate-800">${h.name}</td>
                        <td class="py-2.5 px-3 text-right">
                            <button class="delete-holiday-btn w-8 h-8 inline-flex items-center justify-center bg-white hover:bg-rose-50 text-slate-400 hover:text-rose-600 border border-slate-200 hover:border-rose-200 rounded-lg transition" data-id="${h.id}" data-name="${h.name}" title="Remove holiday">
                                <i class="fa-regular fa-trash-can text-xs"></i>
                            </button>
                        </td>
                    </tr>
                `).join('');
                tbody.querySelectorAll('.delete-holiday-btn').forEach(btn => {
                    btn.addEventListener('click', async () => {
                        if (!confirm(`Remove holiday "${btn.getAttribute('data-name')}"?`)) return;
                        try {
                            await fetchWithAuth(`${API_BASE}/admin/holidays/${btn.getAttribute('data-id')}`, { method: 'DELETE' });
                            showMessage('Holiday removed successfully', 'success');
                            loadHolidays();
                        } catch (err) { console.error(err); }
                    });
                });
            } catch (err) { console.error(err); }
        }

        document.getElementById('add-holiday-btn').addEventListener('click', async () => {
            const date = document.getElementById('holiday-date').value;
            const name = document.getElementById('holiday-name').value.trim();
            if (!date || !name) { alert('Please enter a date and a holiday name'); return; }
            try {
                const response = await fetchWithAuth(`${API_BASE}/admin/holidays`, {
                    method: 'POST',
                    body: JSON.stringify({ date, name }),
                });
                const data = await response.json();
                showMessage(data.message || 'Holiday added successfully', data.success ? 'success' : 'error');
                document.getElementById('holiday-name').value = '';
                loadHolidays();
            } catch (err) { console.error(err); }
        });

        async function loadAuditLogs() {
            try {
                const limit = document.getElementById('audit-limit').value;
                const response = await fetchWithAuth(`${API_BASE}/admin/audit-logs?limit=${limit}`);
                const data = await response.json();
                const tbody = document.getElementById('audit-table');
                const entries = data.data || [];
                if (entries.length === 0) {
                    tbody.innerHTML = `
                        <tr>
                            <td colspan="6" class="py-8 text-center text-slate-400 text-xs font-medium">
                                <i class="fa-solid fa-clipboard-list text-slate-300 text-xl mb-1 block"></i>
                                <span>No admin actions recorded yet.</span>
                            </td>
                        </tr>
                    `;
                    return;
                }
                tbody.innerHTML = entries.map((l, i) => {
                    const time = new Date(l.created_at).toLocaleString();
                    const adminName = l.admin ? l.admin.name : (l.admin_id ? 'Admin (deleted)' : 'System');
                    const target = l.target
                        ? `${l.target.name}<br><span class="text-[11px] text-slate-400">${l.target.email}</span>`
                        : (l.details && l.details.params && (l.details.params.leaveId || l.details.params.holidayId || l.details.params.logId))
                            ? `<span class="text-[11px] font-mono text-slate-400">${l.details.params.leaveId || l.details.params.holidayId || l.details.params.logId}</span>`
                            : '<span class="text-slate-400 text-xs">—</span>';
                    const status = l.details && l.details.status;
                    const statusColor = status < 300
                        ? 'bg-emerald-50 text-emerald-700 border-emerald-200'
                        : status >= 400
                            ? 'bg-rose-50 text-rose-700 border-rose-200'
                            : 'bg-amber-50 text-amber-700 border-amber-200';
                    return `
                        <tr class="hover:bg-slate-50/70 cursor-pointer audit-row" data-index="${i}">
                            <td class="py-2.5 px-3 text-xs font-semibold text-slate-600 whitespace-nowrap">${time}</td>
                            <td class="py-2.5 px-3 text-xs font-bold text-slate-800">${adminName}</td>
                            <td class="py-2.5 px-3"><span class="font-mono text-[11px] font-semibold text-indigo-600 bg-indigo-50 rounded-md px-2 py-1">${l.action}</span></td>
                            <td class="py-2.5 px-3 text-xs font-semibold text-slate-700">${target}</td>
                            <td class="py-2.5 px-3"><span class="inline-block text-[11px] font-bold rounded-md px-2 py-1 border ${statusColor}">${status}</span></td>
                            <td class="py-2.5 px-3 text-right"><i class="fa-solid fa-chevron-down text-slate-300 text-xs"></i></td>
                        </tr>
                    `;
                }).join('');
                tbody.querySelectorAll('.audit-row').forEach(row => {
                    row.addEventListener('click', () => {
                        const entry = entries[parseInt(row.getAttribute('data-index'))];
                        const title = `${new Date(entry.created_at).toLocaleString()} — ${entry.action}`;
                        document.getElementById('audit-detail-title').textContent = title;
                        document.getElementById('audit-detail-body').textContent = JSON.stringify(entry.details, null, 2);
                        document.getElementById('audit-detail-panel').classList.remove('hidden');
                    });
                });
            } catch (err) { console.error(err); }
        }

        document.getElementById('refresh-audit-btn').addEventListener('click', loadAuditLogs);
        document.getElementById('audit-limit').addEventListener('change', loadAuditLogs);
        document.getElementById('audit-detail-close').addEventListener('click', () => {
            document.getElementById('audit-detail-panel').classList.add('hidden');
        });

        loadSettings();
        loadHolidays();
        loadAuditLogs();
    </script>